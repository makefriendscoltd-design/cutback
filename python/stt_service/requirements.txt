# Whisper STT Service Requirements
#
# 주의: torch 는 포함하지 않는다.
# faster-whisper 는 ctranslate2 (CPU/CUDA 네이티브) 를 사용하므로 torch 불필요.
# torch 를 설치/번들에 포함하면 torch/lib/libiomp5md.dll 과
# ctranslate2/libiomp5md.dll 이 중복되어 Intel OpenMP 이중 초기화 충돌
# (STATUS_STACK_BUFFER_OVERRUN, 0xc0000409 fast-fail) 이 발생한다.

faster-whisper>=1.0.0
pyzmq>=25.1.0
# 한국어 자막 맞춤법 교정 (Naver Speller HTTP 직접 호출용)
requests>=2.31.0
